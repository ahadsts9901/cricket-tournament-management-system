import { Standing } from '../types'

const DataTable = ({ data }: { data: Standing[] }) => {
    console.log(data)
    return (
        <div className='mt-4'>
            DataTable
        </div>
    )
}

export default DataTable
